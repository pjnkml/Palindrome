Palindrome
This an easy python script  to check whether the word is a palindrome. A palindrome is a word, number, phrase, or other sequence of characters which reads the same backward as forward. In this example the functiion is checking a single world palindrome. 

In order to check a different word , change the value of str variable in the script
